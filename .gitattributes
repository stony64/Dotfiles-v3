# ------------------------------------------------------------------------------
# FILE:          .gitattributes
# VERSION:       1.3.0
# DESCRIPTION:   Line-Endings + Binary Policy (Proxmox/Dotfiles/LaTeX + exec).
# AUTHOR:        Stony64
# ------------------------------------------------------------------------------

# --- GLOBAL AUTO DETECT + LF DEFAULT ------------------------------------------
* text=auto eol=lf

# --- SHELL/DOTFILES FRAMEWORK (EXECUTABLE + LF) -------------------------------
*.sh                text eol=lf exec
.bash*              text eol=lf
dotfilesctl.sh      text eol=lf exec
core.sh             text eol=lf exec
.dircolors          text eol=lf
.nanorc             text eol=lf
.shellcheckrc       text eol=lf
.bashwartung        text eol=lf exec

# --- CONFIGURATION (LF) -------------------------------------------------------
*.{json,jsonc,yaml,yml,toml,conf,cfg,ini} text eol=lf
.editorconfig       text eol=lf
.gitignore          text eol=lf
.gitattributes      text eol=lf
*markdownlint*      text eol=lf
bindings.json       text eol=lf
settings.json       text eol=lf

# --- DOCUMENTATION (LF) -------------------------------------------------------
*.{md,txt,mdx}      text eol=lf
project-words.txt   text eol=lf
LICENSE             text eol=lf
README*             text eol=lf
CHANGELOG*          text eol=lf

# --- PROXMOX/MC/ZFS (CONFIG + DATA) -------------------------------------------
*.pve               text eol=lf
mc/ini              text eol=lf
*.conf.d            text eol=lf
*.zfs               binary
*.pool              binary

# --- LATEX (LF) ---------------------------------------------------------------
*.{tex,latex}       text eol=lf

# --- WINDOWS FILES (CRLF) -----------------------------------------------------
*.{bat,ps1,cmd}     text eol=crlf

# --- MAKEFILES (TAB HEILIG!) --------------------------------------------------
[{Makefile,makefile,*.mk}] indent_style=tab indent_size=8 trim_trailing_whitespace=false

# --- BINARY/MEDIA (NO TRANSFORM) ----------------------------------------------
*.{png,jpg,jpeg,gif,webp,ico,svg,pdf,zip,7z,gz,tgz,lz4,rar,deb,rpm,AppImage,snap} binary
Cargo.lock          text eol=lf
package-lock.json   binary
yarn.lock           text eol=lf

# --- DEV TOOLS (LOCKFILES) ----------------------------------------------------
*.lock              binary
.env.*              text eol=lf

# --- GIT HUB ACTIONS ----------------------------------------------------------
.github/workflows/*.yml text eol=lf
